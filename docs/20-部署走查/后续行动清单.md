# 后续行动清单

> ⚡ 高优先（改机制/代码） · 📄 中优先（流程契约） · 已生效原则 · 待查。来源标（学-xx / 站 / 缺口）。

## ⚡ 高优先

- [x] **站 1（完成）**：提交清单确认 → commit `f85e115` → push main → status 干净（来源：学-02）
- [x] **站 1（完成）**：`.gitignore` 加 `.cursor/plans/`（学-12）
- [ ] **站 2（计划）**：内容全面重写——删过渡内容 → 重写后须有一篇可读中文长文（学-13；**上线前最后步骤**，不阻塞阶段 3）
- [x] **0021 控制台**：只上传（含 private）、禁止残树 `--delete`；备份从服务器打包；去掉日常推曲库（学-14）
- [x] **0022 运行时**：口令不能读 private / 不能改删；WebAuthn `device`；新建配额。Agent token 过期仍后置

## 📄 中优先

- （站 3 确认 env 前置后补：owner:password / session secret 本机生成、离线保存）

## 已知债（本轮不做，上线后按信号触发）

- 浏览模式（藏终端 + 导航）——触发信号：有人进得来但不会 `open`（docs/22 §0）
- 阶段 1「访客靠命令」张力——先接受，用现有 open 阅读面证明内容可读，上线后再 browse（用户判定）
- 产品句 / 主题 / CLI 手感 / 评论 / member / directories 洞 / 文档体系化（学-09）
- Caddy/容器/灰云实操（阶段 0.3/7/8–11 置后，只理解不执行）
- 阶段 6 扩展验证（跳过：缺现实信号，不是缺契约自信）
- smoke:ui 回归（改 UI 后才需要）

## 待查

- `.cursor/rules/中枢状态板.mdc` 改动是否随上线批提交（用户拍板）
