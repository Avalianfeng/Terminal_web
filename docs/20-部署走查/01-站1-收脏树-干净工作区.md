# 站 1 · 收脏树 → 可上线 commit

> 对应清单：[`docs/20`](../20-部署前自评清单.md) 阶段 2.1（仓库卫生）。
> 为什么先做：上机的是**已验证的 commit**，不是工作区脏树；阶段 2.1 要求 `git status --porcelain` 干净。不先收树，边勾清单边改文件会把「上线前置」和「顺带调整」混进同一坨 diff。

## 当前状态（2026-08-21 探查）

分支 `main`，HEAD `1b51a30`，工作区 12 项（9 改 + 3 新增）：

**已跟踪修改（9）**
| 文件 | 性质 | 判定 |
|------|------|------|
| `scripts/list-publish-paths.ts` | **新增**（??） | 提交：publish 白名单验收工具（阶段 5 直接用） |
| `docs/22-上线后方向.md` | **新增**（??） | 提交：docs 解耦配套（用户判定） |
| `docs/00 / 09 / 13 / 20 / 21 / adr/0018` | 解耦/配套修改 | 提交（用户判定） |
| `package.json`（+1 行，list:publish-paths 脚本） | 配套 | 提交 |
| `AGENTS.md`（+3/-1） | 配套 | 提交 |
| `.cursor/rules/中枢状态板.mdc`（+4/-4） | 中枢治理改动：子 Agent 不读状态/开场读状态分流 | **随批提交**（检查题确认） |

**未跟踪新增（3）**
| 文件 | 判定 |
|------|------|
| `scripts/list-publish-paths.ts` | 提交（见上） |
| `docs/22-上线后方向.md` | 提交（见上） |
| `.cursor/plans/访客本地mp3轨_073473d3.plan.md` | **不进 Git**（用户拍板：Cursor 计划记录，非上线配套） |

## 机制与原则（本站在场记录）

1. **收树优先原则**：先出可上线 commit，再勾清单；修只修阻塞上线的（用户）。
2. **`.cursor/plans/` 缺口**：未被 `.gitignore` 覆盖 → `git status` 永远有 untracked，阶段 2.1「干净」达不成。建议 `.gitignore` 加一行 `.cursor/plans/`（待确认后改）。
3. **提交内容 = 上线配套**：list-publish-paths + docs 解耦（用户点名）；中枢规则改动是否随批由用户定。

## 站 1 待办

- [x] 用户确认提交清单：中枢规则**随批提交**（检查题 1）
- [x] `.gitignore` 加 `.cursor/plans/`（检查题 2）
- [x] 打包方式：**一个 commit**（检查题 3）
- [x] commit + push → `git status --porcelain` 干净（仅剩 gitignore 项）

## 检查题（记录）

| # | 题 | 回答 | 判定 |
|----|----|------|------|
| 1 | 中枢状态板.mdc 已跟踪改动怎么处理 | 随批提交 | 与 AGENTS.md 子 agent 政策配套，一起入库保持一致 |
| 2 | `.cursor/plans/` 不进 Git 怎么落实 | `.gitignore` 加行 | 声明计划记录不入库；status 干净达成 2.1 |
| 3 | 打包方式 | 一个 commit | 单一意图「上线配套」，VPS 拉一个 hash 到位 |
