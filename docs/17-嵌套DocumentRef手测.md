# 嵌套 DocumentRef 手测（方案 A）

> **角色**：主人统一验收清单。实现过程中由 Agent 补全条目；**真实验证由主人执行**。  
> **对应**：内容协议（2026-08-17）方案 A；ADR 修订见落地提交。  
> **状态**：骨架 — 随实现补步骤与期望。

## 前置

- [ ] 本地 `npm run dev` 可起
- [ ] 本机 owner（local-dev）或已 `login`

## 身份与读写

- [ ] （待补）多段 `localKey` 创建 / 打开 / 编辑 / 删除
- [ ] （待补）扁平旧路径仍可用（兼容）

## VFS / 终端

- [ ] （待补）`cd` / `ls` / `tree` 进入子目录
- [ ] （待补）`mkdir` / `rmdir`（删目录确认从简）

## 发现层 HTTP

- [ ] （待补）`GET/PUT/PATCH/DELETE` 多段 `localKey`

## 明确不做（本清单不测）

- 内容同步 push/pull 工具
- 对话公开展示
