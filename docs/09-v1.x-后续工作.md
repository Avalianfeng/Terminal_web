# v1.x 后续工作清单

> 状态：§0–§2.1 已收口；**2026-08-11 治理刀**：§3 按威胁分级、§5 重排、[`00-文档入口`](00-文档入口.md) 地图落地；§2.2/§2.3 刻意后置  
> 相关：入口地图 [`00`](00-文档入口.md)；路线 [`06`](06-v1.0-路线纲要.md)；契约 [`08`](08-发现层对象模型.md)；写验收 [`10`](10-agent-写API验收.md)；edit 手测 [`11`](11-终端edit手测清单.md)；身份手测 [`12`](12-站点身份手测.md)；部署 [`13`](13-cylf.me-部署.md)  
> 用途：把「下一刀」写成可勾选条目，避免散落在聊天与过时状态文件里

---

## 阶段小结（2026-08-10 · §0–§1 / 2026-08-11 · §2.1）

| 块 | 交付 | 过关信号 |
|----|------|----------|
| §0 缺陷 | edit 路径 / 面板读 raw / 文档对齐 | A：`edit projects/…` + 写 API 抽查 |
| §1.1 | `scripts/smoke-write-api.mjs` + `docs/10` + npm/AGENTS/README | 本机 + 子 agent：401/403/201/409/405/DELETE 全绿 |
| §1.2 | `docs/11` 手测表 + 路径/新建/删/失败提示抽查 | Playwright 抽查 2026-08-10 全绿 |
| §2.1 | status/tags 进索引/详情（可选）+ PATCH 三态 + 过滤/投影 + 写响应完整文档 | 2026-08-11 契约层收尾提交 + smoke（含 PATCH/过滤断言）全绿 |

**本阶段刻意未做（产品扩展）**：§2.2 / §2.3 拍板与实现。§2.1 落地详情见 [`08`](08-发现层对象模型.md) §5.8/§7。  
**治理**：文档迷路先看 [`00`](00-文档入口.md)；债从哪抽看下方 §3 / §5（不再默认下一刀 = 外源）。  
**更完整的 WIP 真相**（验收证据、复跑口令、脏面分类）：中枢 `状态/项目/my_web.md`——不进远程。

---

## 0. 刚修的缺陷（已完成 · 记一笔）

| 项 | 说明 |
|----|------|
| `edit` 路径解析 | `edit thoughts/foo`、`edit /projects/foo` 曾被整段 `SLUG_PATTERN` 误拒（08-08） |
| Tab 补全 `edit` | `command-registry` / 路径补全已收录 |
| **编辑器读失败** | `actions.ts` 的 `toResult` 成功时丢弃返回值 → `getDocumentRaw` 只回 `{ ok: true }` 无 `raw`；面板走到「读取失败」（08-09 修） |
| 文档「C 挂起」残留 | `06`/`08`/`07`/`AGENTS`/`my_web.md` 现行表述已对齐 |
| HTTP 写后 RSC | `PUT`/`DELETE items` 后 `revalidatePath("/")` |

手测建议：`edit projects/agent_personification` 应能看到 frontmatter + 正文；保存后 `open` 一致。

---

## 1. 立刻可做（低设计成本）

### 1.1 Agent 验收包固化

- [x] 将写闭环 curl smoke（401 / 403 / 201 / 409 / 200 DELETE / 405）收成可重复脚本（如 `scripts/smoke-write-api.mjs`）
- [x] 短 playbook：发现 → 索引 → 详情（含 `hash`）→ `If-Match` 写 → 冲突 → 删除（[`10-agent-写API验收.md`](10-agent-写API验收.md)）
- [x] 在 `AGENTS.md` 或 README 链到脚本用法（`npm run smoke:write-api`）

**为何优先**：无自动化测试套件时，这是契约回归最划算的护栏。

### 1.2 终端 `edit` 回归手测清单

- [x] `edit projects/<已有>` / `edit thoughts/<已有>` / `edit /projects/<slug>`
- [x] `cd thoughts` + `edit <slug>`；裸 slug 新建
- [x] 保存后 `open` / `cat`；删除后索引消失（盘上文件 + refresh）
- [x] `edit timeline` / `edit`（无参）仍应失败且提示清楚

完整表：[`11-终端edit手测清单.md`](11-终端edit手测清单.md)（2026-08-10 抽查勾选）。

### 1.3 终端 CLI 排放（ADR 0012）

- [ ] 落地 `lib/archive/cli-emit.ts` + message id/slots；`commands.ts` 与 `archive-terminal.tsx` 共用 emitter
- [ ] **首刀 music**：download `\r` progress + `saved '…' (id.ext)`；修 `music playlist next` 空冒号；子命令缺参只出 `usage: music playlist …`，不 dump 整页 help
- [ ] 单测锁定 canonical 格式（如 `music: no track matches '爱请'`）
- [ ] （可选后续）sweep 档案命令（`ls`/`invalidPath` 已是样板）

**为何现在**：0012 已拍板，设计成本已付；music 域 bug（空冒号、匿名 download、百科 usage）可直接对照 ADR 修。

---

## 2. 应先思考再动手（产品/契约拍板）

### 2.1 HTTP 索引是否暴露 `status` / `tags`

| 现状 | 决策（2026-08-11） |
|------|------|
| 阅读面板有 status/tags；写 API body 可带 status/tags | **补字段**：索引与详情均提供（可选字段，外源友好） |

- [x] Deep Dive：补字段进索引/详情，**或** 明确文档「Agent 不读元数据、只读正文」→ 拍板**补**
- [x] 落地：`08` §3.1/§5.3/§5.4/§5.6 + `api-read.ts` 转换；新增 PATCH 部分更新（§5.8）+ 索引过滤（`status` 单值 / `tag` 多值 AND / `fields` 投影）+ 写响应返回完整文档；详见 `08` §7 落地记录

### 2.2 外源 / 下一 `source`（如 github）

- [ ] Deep Dive：发现表、`localKey` 形态、转发 vs 镜像、失败与 `planned`
- [ ] 实现前锁身份与错误模型；勿先堆适配器

### 2.3 其它 `kind`（image / music / video）

- [ ] 仍 planned；先确认是否真要进发现层，还是站外链接即可
- [ ] 有真实内容样本再开，避免空枚举

---

## 3. v1.x 工程债（按威胁分级）

> 分级目的：避免「有需要再开」变成平铺愿望单。抽债时先问：**错数据 / 看不懂 / 只是能力不够？**

### 3.0 安全与部署（权威原则 → [`adr/0007`](adr/0007-security-deployment-posture.md)）

> **现状（2026-08-15）**：网页主语 [0010](adr/0010-site-principal.md) 已落地（visitor/owner session）。公网闸门其余项见 §3.4。  
> 加写功能前读 0007 §3 纪律 A–E 与 0010，避免与 HTTP 鉴权矛盾。

### 3.1 威胁正确性（读写可能静默错或丢更新）

| 项 | 说明 | 建议时机 |
|----|------|----------|
| 终端 `edit` 接 `expectedHash` | 与 HTTP `If-Match` 对齐；否则人机同改可能互相覆盖 | **已落地**（2026-08-11）：写内核统一 `DocumentRef` + `expectedHash`；编辑器读 hash、冲突可重载 |

| 写后客户端自动 refresh | 已有 `revalidatePath`；已打开阅读面板仍可能陈旧 | **已落地**（2026-08-11）：edit 保存后即时替换 ReadingSurface + `router.refresh` 重绑；删除仍关掉该文。Agent 同页另议 |


### 3.2 威胁理解度（不修也能跑，但自己/Agent 易迷路）

| 项 | 说明 | 建议时机 |
|----|------|----------|
| ~~仓内 docs 入口地图~~ | [`00-文档入口.md`](00-文档入口.md) | **2026-08-11 已落地** |
| cylf.me 发布流程未进仓 | 运维步骤只在脑内 / 站外 | **短 runbook 已落** [`13`](13-cylf.me-部署.md)；实例仍未部署 |
| ~~全屏按钮占位~~ | 曾误记为「假能力」；终端壳「全屏」已实现（`archive-terminal` + `is-terminal-fullscreen`；README 已标已做） | **已落地**（勿再当债）；勿与 Cursor Agents 页右上角全屏混淆 |
| `.thinking-advisor/` sessions | gitignore；不挡仓 | 妨碍检索时再归档；不以「清空历史」当主线 |

### 3.3 可延后（能力缺口；无刚需不开）

| 项 | 说明 | 建议时机 |
|----|------|----------|
| HTTP 全文 / 路径检索 | 终端有 `search`/`find`，HTTP 无等价 | Agent 扫读明确不够用时 |
| 索引分页 `?limit=` / `?offset=` | 响应 shape 变化，需 apiVersion 评估（`08` §4） | 外源条目数破百 |
| 排序 / 时间戳元数据 | 无 `created`/`updated`；牵动解析、写盘与存量迁移 | 加元数据模型时（**新功能级**，不入收尾） |
| 完整权限 / 审计 / 多人 session | 超单主人 0010 + Bearer+scope | 多人 / 生产加固时 |
| 写后触发（webhook / 重建） | 未定义 | 有外部消费者时 |

### 3.4 公网部署闸门（见 0007 / 0010）

> 身份与 UI 写闸已按 [0010](adr/0010-site-principal.md) 落地。本地曲库 [0011](adr/0011-music-local-cache-public.md) 让访客播已落盘媒体。首次 deploy 仍须配 env、同步 `data/music/`，并跑 [`12`](12-站点身份手测.md) / [`13`](13-cylf.me-部署.md) / [`14`](14-music-本地曲库手测.md)。

| 项 | 说明 | 状态 |
|----|------|------|
| UI 写 = owner principal | visitor 无 `edit`；Actions 硬拒；local-dev implicit owner | [x] |
| Markdown `href` 白名单 | `markdown-prose.tsx`：`http`/`https`/`mailto`/相对路径 | [x] |
| 写 API CORS 收紧 | `api-http.ts` 读写分策略 | [x] |
| cylf.me 短 runbook | [`13`](13-cylf.me-部署.md) | [x] |
| 闸门手测 | [`11`](11-终端edit手测清单.md) + [`12`](12-站点身份手测.md) | [x] |
| 本地曲库（0011） | visitor 只播 `data/music/`；owner download / 流式 | [x] |

---

## 4. 明确不做（当前仍成立）

- 多媒体播放器 / 图库产品化
- 完整站内富文本编辑器、多入口精修 UI
- 进场 `whoami` 剧本、第二套非终端首页
- 无鉴权可写；把 Cursor CLI 写成唯一编辑方式
- 编排中枢当眼前主线
- 为「文档显得完整」重写 `01`–`05`/`07` 过程稿（地图标明角色即可）

---

## 5. 推荐下一刀顺序

1. ~~**1.1 Agent 验收包**~~（`smoke:write-api` + `docs/10`）  
2. ~~**1.2 终端 edit 手测清单**~~（`docs/11`）  
3. ~~**2.1 status/tags 策略拍板**~~（2026-08-11 落地）  
4. ~~**治理刀**~~（2026-08-11）：`00` 入口地图 + 本文件 §3 分级 + §5 重排  
5. ~~**从 §3.1 抽正确性债**~~（`expectedHash` + 写后面板 refresh；2026-08-11 已落地）  
6. ~~**Cloud Agent 环境**~~（2026-08-12）：仓内 `.cursor/environment.json` + 云端不读本机中枢约定（PR #4）  
7. ~~**§3.4 / 0010**~~：站点身份 + 公网闸门 ——**2026-08-15 落地**；deploy 时按 [`13`](13-cylf.me-部署.md) 配 env  
8. ~~**0011 本地曲库**~~ ——**2026-08-15 落地**；手测 [`14`](14-music-本地曲库手测.md)  
9. **§1.3 / [0012](adr/0012-cli-output-contract.md) CLI 排放** ——**0010/0011 后的默认下一刀**（先 music emission，再可选 archive sweep）  
10. **§2.2 外源 Deep Dive**（身份/`localKey`/转发 vs 镜像）——**有外源刚需再开**  
11. §2.3 其它 kind、§3.3 可延后项——有真实样本或规模信号再开  

拍板后把选中的项写进中枢焦点（或可选 `.thinking-advisor/sessions/`）；**本文件保留为总清单**，避免第三套进度正文。

---

## 6. 待办记录（2026-08-11 落盘 · 暂不执行）

> 收尾时明确不做的 C/D 组内容先记在这里，防止丢失方向；等 §2.1 收尾确认后再评估。

### C 组：收尾清理候选

- [x] 删 `toItemPayload` 空 hash 死代码（`lib/archive/api-read.ts`，2026-08-11 清理提交）
- [x] 删除 `.thinking-advisor/状态.md`（与中枢重叠且过期；sessions 历史场次保留）
- [x] 校正 `docs/09` §2.1 旧表述「终端列表有 status/tags」（§2.1 收尾已改写为阅读面板）
- [x] 修正 `docs/06` 变更日志「C 挂起」历史句（注明后已重开完成）

### D 组：新功能方向候选（后置；见 §5 第 7–8 步）

- [ ] §2.2 外源 / 下一 `source`（如 github）：发现表、`localKey` 形态、转发 vs 镜像、失败与 `planned`；实现前先锁身份与错误模型，勿先堆适配器
- [ ] §2.3 其它 `kind`（image / music / video）：有真实内容样本再开，避免空枚举
- [ ] §3.3 HTTP 全文检索等：能力债，Agent 扫读明确不够用时再开（勿与 §3.1 正确性债混排）
