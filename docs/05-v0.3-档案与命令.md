# v0.3 档案与命令

> 状态：批量 open / find / status / help 已落地；**产品主线已转向视觉/体验**（04 抛光一轮）；内容加厚非核心  
> 前提：v0.2 空间分层（终端 + 外区阅读 + rail）已够用  
> 相关：`01-v0.2-工作区图景.md`、`04-外区设计读法.md`

---

## 1. 一句话

**v0.2 证明空间；命令侧已够用；当前核心是 Dual Phase 体验（视觉 / 动效 / 可接受的丰富），不是补 markdown。**  
内容与目录 about 有空再补；不做第二套非技术站、不做进场剧本、不做身份编辑。

---

## 2. 产品句（钉死）

用终端交互替代传统布局的个人站。  
文件系统是分类隐喻；**档案是对象，终端是探索器**——多层是设计。

---

## 3. 本版范围

| 做 | 不做 |
|----|------|
| ~~`open` 批量 / find / status / help~~ | 复杂 Person 对象模型大重构 |
| Dual Phase 视觉抛光（见 04） | 外区动效库、手机第二套 IA |
| （随后）体验收束：轻量入口等 | 首次进入完整叙事（仍延后） |
| 内容有空再补 | 身份系统 + 多入口编辑 |

---

## 4. 目录约定（少目录、深材料）

当前顶层保持：

```text
/
├── person
├── projects/
├── thoughts/
└── timeline
```

- 新顶层目录**只在有真实内容时**加（如 `notes/`），不预留空架子。  
- 每个顶层目录目标：有可 `open` 的节点，而不是只有列表命令。  
- `open <目录>` / `open .` / `open *`：打开该目录下可阅读节点（见 §5），不是报「是目录」。

---

## 5. `open` 语义（本刀已定）

| 输入 | 行为 |
|------|------|
| `open <slug\|文件路径>` | 单篇 → main（旧 main demote 进 rail） |
| `open a b c` | 多目标：去重后**最后一项 → main**，其余进 rail |
| `open .` / `open *` | 当前目录下可阅读节点；**首项 → main**，其余进 rail |
| `open projects` / `open thoughts` | 同上，对该目录批量打开 |
| `open timeline` / `open person` | 保持：timeline → 阅读面板；person → 终端摘要（暂不进面板） |

硬顶：主槽 1 + rail ≤ 8；超出截断并提示。  
`prefers-reduced-motion`：批量仍瞬时换位，不串场秀。

### 5.1 `cat` vs `open`

| 命令 | 去向 |
|------|------|
| `cat <节点>` | **终端**纯文本；超一屏后 Enter/Space 每次一行，`q`/`Esc` 退出 |
| `open …` | **外区**阅读面板（排版 / 批量 / rail） |
| `timeline` | 仍走外区（与 `open timeline` 一致） |

---

## 6. 随后命令

- ~~`find`~~：路径 / 名称检索；空参列出可打开节点；结果可再 `open`
- ~~`status`~~：人物 / 计数 / 索引时间（档案感）
- ~~help~~：探索 / 阅读 / 会话 分组
- 目录说明文件（如可 open 的 about）：有文再挂进 VFS
- 内容加厚（与命令并行）

---

## 7. 成功标准（v0.3 末）

- 连续探索约 5 分钟，不会三步走空。  
- `open thoughts` / `open a b` 行为符合预期，rail 不爆。  
- 进场叙事仍可继续延后；不因「命令变多」而提前做引导动画。

---

## 8. 落地记录

- 文档建立。
- 批量 `open`：多目标 / 目录 / `.` `*`；`openReadingMany`；help/i18n/complete 对齐。
- `find` / `status`；help 三分组；Tab 补全含新命令。
- 视觉抛光一轮（纸面优先 + 窄屏必过）：见 `04`。
