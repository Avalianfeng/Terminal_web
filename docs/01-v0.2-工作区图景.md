# v0.2 工作区图景

> 状态：空间分层 + 开合/rail 已落地；**视觉抛光一轮已完成**（见 04）；命令见 `05`；内容非主线  
> 目的：钉死空间分层与迭代边界，不写进场剧本、不做完整窗管。

---

## 1. 四层分工

| 层 | 名称 | 职责 | v0.2 |
|----|------|------|------|
| A | 页面工作区 | 浅色外区承载阅读；终端是页面主体区域之一，可滚出视口 | 视觉抛光一轮 |
| B | 终端表面 | 探索与命令；保持终端感；运行时自有，不接真 Linux | Tab 已做；xterm 档位 1 在 `feat/xterm-surface` |
| C | 阅读表面 | `open` 等打开档案后，在外区出现独立面板 | 纸面层级 + 窄屏必过 |
| D | 命令 / 内容 | 分类、进场引导、人格命令 | 命令已增强；**内容加厚非核心**（见 05） |

**产品意图**：展示本人档案，不是留存漏斗。

---

## 2. C0 约定（已落地）

### 外区

- 浅亮阅读场（衬出窗口边界），**不再**整页等同黑终端。
- 关键词：**Surface**（面）、**Elevation**（抬升：谁在谁上面）。
- 外区 UI 方向与禁区：见 `04-外区设计读法.md`（先约束，再动视觉）。

### 终端

- 仍是主体交互；打开文档后**仍可输入命令**。
- Prompt 随 cwd：`visitor@archive:~$` / `~/projects$`。
- Tab 补全：命令名、路径、`open`/`cat` 的 slug（prefix → LCP → cycle）。
- 表面与运行时分离：表面可接 `xterm.js`；命令仍走 Archive runtime。**不嵌 Linux。**

### 阅读面板（C0 → Phase 2b）

- `open` / `cat`（文档）/ `timeline` → 外区独立面板；`open timeline|person` 可用。
- 布局：终端上；其下 `main` 主槽 + `rail` 已打开列表（桌面右侧 / 窄屏横向条）。
- 换文：旧 main 短 scale+位移 demote 进 rail；新 main 仍用 Phase 1 进场（非 FLIP）。
- 关闭：主槽 × / `Esc`（有 rail 则晋升）；rail 小块可单独移除；`clear` 全关。
- **原则**：终端尺寸稳定；页面滚动承载阅读区；区块优先于动作。
- **未做**：贴边 dock、拖拽窗管、「从终端涌出」。

### 动效

- 不做首次进场 `whoami` 动画。
- 部件级：输出短间隔、面板进出。
- 遵守 `prefers-reduced-motion`。

---

## 3. 明确不做（当前）

- 进场引导剧本
- 复杂档案对象模型重构
- **身份系统 / 登录**（与编辑一并延后，见 §5）
- AI / 多人
- 真 Linux / WebVM
- 窗管贴边 / 拖拽（`rail` 已做多文件槽；不做真窗管）

---

## 4. 后续演进顺序

1. ~~Tab 补全~~（已做）
2. ~~评估 xterm.js~~ → 见 `02-xterm终端表面评估.md`
3. ~~终端表面接入 xterm（档位 1）~~ → 分支 `feat/xterm-surface`（单缓冲；阅读面板不动；复用 `complete.ts`）
4. ~~外区设计读法~~ → `04-外区设计读法.md`（质感 C0 冻结；区块优先）
5. ~~开合 Phase 1~~ → 主槽展开/收起 + 滚动焦点编排
6. ~~Phase 2a `rail` 区块~~ → `main`+`rail` 状态与布局（见 04 §6）
7. ~~Phase 2b demote~~ → 旧主槽 scale+位移进 rail；新主槽 Phase 1 进场
8. **v0.3 档案与命令** → `05-v0.3-档案与命令.md`（目录 / 批量 open / 内容加厚）
9. 首次进入叙事（等内容够厚后再做）
10. **身份系统 + 多入口编辑**（见 §5，捆绑，不提前做半套）

---

## 5. 延后：身份系统 + 多入口编辑

> 只定方向，本阶段不实现。

### 原则

- **编辑与身份绑定**：无身份则只有公开只读；有所有者身份才能写。
- **多入口编辑**：同一档案对象可从多处进入编辑，而不是只做一个隐藏后台。

### 可能入口（未来）

| 入口 | 说明 |
|------|------|
| 终端命令 | 如 `edit <slug>` / `append thoughts/...`（仍走 runtime） |
| 阅读面板 | 所有者可见「编辑」→ 面板内或旁路编辑态 |
| 内容文件 / 将来 API | `content/` 或远端存储的正式源；终端与面板都是视图 |

### 不提前做的原因

- 写路径会强迫存储、冲突、权限模型定稿；现在内容形态未稳。
- 半套「能改但不能鉴权」比只读更危险。

### 与 xterm / AI 的关系

- 身份就绪后，AI/脚本批量命令才有「以谁身份执行」的主语。
- 在此之前，终端表面可先加强只读探索与流式**展示**。

---

## 6. 设计用词（描述三块表面时用）

对每一块只答四问：**背景 / 边界 / 焦点 / 开关**。

- Surface / Elevation / Spatial separation / Containment / Focal point

---

## 7. 落地记录

- 外区浅亮工作场；终端独立深色壳。
- 视觉抛光一轮：纸面/等宽字体分离、prose 收拢、窄屏 safe-area 与触控、终端小屏压高（见 04）。
- 终端 fullscreen：壳 `fixed` 铺满视口；Esc / 再点退出；`open` 打开阅读时自动退出全屏（Dual Phase）。
- 终端 tone → xterm 16 色 ANSI；`/themes` 增加 tone 预览（不加字体超市）。
- `open` / `cat` / `timeline` → 终端下方阅读面板；高度随内容，页面滚动。
- 另一 agent：`open timeline|person`、目录拒绝 open、失败提示真实 slug、help 补目录命令、动态 prompt、姓名「策月帘风」。
- Tab 补全：`lib/archive/complete.ts` + 输入区候选提示。
- xterm：档位 1 接入见分支 `feat/xterm-surface`。
- 布局纠偏：放弃「上下抢视口 + FitAddon rows-1 常数」；终端尺寸稳定，`lineHeight=1` + 普通 `fit()`。
- 已合并：`experiment/workspace-palette`；并做**接缝**：`PaletteBootstrap` 首页读 localStorage、`xterm` 跟 `data-palette` CSS token。
- Git/PR 与主-子 worktree 纪律已记入 `Desktop/agent/draft.md`（待消化）。
- 外区开合 Phase 1：主槽展开/收起 + 退场与滚回终端并行；质感冻结。
- Phase 2a：`ReadingState` + `ReadingRail`；再 open 进 rail；关主槽可晋升；桌面右侧 / 窄屏横条。
- Phase 2b：`ReadingDemoteGhost` + rail `is-arriving`；换文 demote 与新主槽进场并行；`prefers-reduced-motion` 瞬时换位。
