# 发现层对象模型（设计锁定）

> 状态：**对象模型 + HTTP 契约形状已锁定并实现**（2026-08-06）——现行线上契约以此为准  
> 来源：thinking-advisor Reassessment → Deep Dive（对象模型）→ Deep Dive（契约形状）收口  
> 相关：路线校准见 [`06-v1.0-路线纲要.md`](06-v1.0-路线纲要.md) §2 / §4 / §7；旧契约 [`07-公开读API.md`](07-公开读API.md) 已被本文替代

---

## 1. 为何有本文

最小 v1.0 已完成纯文本呈现（A）与公开读 API（B）。原「下一刀 C 鉴权写」**挂起**。

校准后的满意主锚是**富读中枢**：站作为可被丰富发现与读取的中心；外源（如 GitHub）与其它类型后挂。第一刀想象是**发现层先统一**——先钉「一条可读物」长什么样，再谈契约形状落地与具体外源。

本文只锁**对象模型**。不代替 `07` 的现行 HTTP 字段；实现时可另开版本或演进端点。

---

## 2. 产品对齐（一句）

发现层对外呈现**统一条目**；首版基础类型只有 **`document`**。身份用 **`source` + `localKey`**（复合权威）。索引带可读摘要；详情在索引之上加正文。

---

## 3. 统一条目

### 3.1 索引（列表 / 发现扫读）

每条至少：

| 字段 | 必填 | 说明 |
|------|------|------|
| `source` | 是 | 来源命名空间；站内文档为 `local` |
| `localKey` | 是 | 源内稳定键；`local` 下 ≈ 现行 `path`（如 `thoughts/archive-system`） |
| `kind` | 是 | 首版仅允许 `"document"` |
| `href` | 是 | 跟随到详情的 URL（实现时定） |
| `title` | 是 | 扫目录可读 |
| `summary` | 否 | 可选摘要 |

**不进最小集**（可后加、可省略）：`status`、`tags`、派生 `id` 字符串。

**身份权威**：`source` + `localKey`。若响应附带拼接 `id`，仅为便捷，**不是**唯一权威。

### 3.2 详情（跟随 `href`）

在索引字段之上，`kind: document` 增加：

| 字段 | 说明 |
|------|------|
| `body` | 正文 |
| `bodyFormat` | 如 `"markdown"`（与现行读 API 对齐） |

### 3.3 与现站内容的映射（约定）

| 现站 | 发现层 |
|------|--------|
| `projects/*`、`thoughts/*` 文档 | `kind: "document"`，`source: "local"`，`localKey` = path |
| `person` | **暂不进**统一条目 |
| `timeline` | **暂不进**统一条目 |

---

## 4. 首版范围与明确后置

**首版只做**

- 统一条目形状（上表）
- 唯一 `kind`：`document`
- 站内 `source: "local"`

**明确暂不进入对象模型 / 条目列表**

- `person`
- `timeline` / `timeline_entry`
- 外源实体（GitHub 等）
- 鉴权写、编排中枢、复杂权限

**能力表预留（非条目）**

发现文档（未来演进的 discovery）可列出 planned 能力/源，例如：

```text
kinds:      document (available) | person | timeline | … (planned)
sources:    local (available) | github | … (planned)
write:      not in scope for this model doc（见 06：C 挂起）
```

占位用 `status: "planned"` 即可；**不要**为未接入源伪造索引条目。

---

## 5. HTTP 契约形状

对象模型统一后，契约也统一为一条面（不并行双轨）。`07` 的旧端点已被替换。

### 5.1 端点表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/v1` | 发现：版本、能力（kinds/sources）、资源表 |
| GET | `/api/v1/items` | 统一条目索引（支持 `?kind=` / `?source=` 过滤） |
| GET | `/api/v1/items?source=local&localKey=…` | 条目详情 |
| GET | `/api/v1/docs?path=…` | 旧 path 语法糖，映射到 `source=local&localKey=…`，返回相同 08 形状 |
| GET | `/api/v1/person` | 人物记录（永久旁路资源，不收进 items） |
| GET | `/api/v1/timeline` | 时间线（永久旁路资源，不收进 items） |

### 5.2 Envelope

成功：

```json
{
  "ok": true,
  "apiVersion": 1,
  "generatedAt": "<iso>",
  "data": {}
}
```

失败：

```json
{
  "ok": false,
  "apiVersion": 1,
  "error": "<code>",
  "message": "…"
}
```

错误码：`not_found`（404）、`bad_request`（400）、`method_not_allowed`（405）。预留：`unauthorized`、`forbidden`、`conflict`。

### 5.3 索引形状

`GET /api/v1/items` → `data.items[]`：

| 字段 | 必填 | 说明 |
|------|------|------|
| `source` | 是 | `"local"`（available）、`"github"`（planned） |
| `localKey` | 是 | 源内稳定键；local 下 ≈ 文件 path |
| `kind` | 是 | `"document"`（available）、`"image"` `"music"` `"video"`（planned） |
| `title` | 是 | 扫目录可读 |
| `summary` | 否 | 可选摘要 |
| `href` | 是 | `GET /api/v1/items?source=local&localKey=…` |

### 5.4 详情形状

索引字段 + `body`（markdown 正文）+ `bodyFormat`（固定 `"markdown"`）。

不保留旧字段：`slug`、`path`、`status`、`tags`。

### 5.5 校验规则

- `source` 和 `kind` 是校验枚举。未知值 → 400 `bad_request`，消息列出 available 值。
- `source` + `localKey` 同时提供 → 详情模式；均不提供 → 索引模式；单独 `localKey` → 400；单独 `source` → 按 `source` 过滤索引。
- 发现文档 `/api/v1` 列出 `kinds.available` / `kinds.planned` 和 `sources.available` / `sources.planned`。

### 5.6 与 `07` 的差异

| `07` 端点 | 处置 |
|-----------|------|
| `/api/v1/archive` | 移除，由 `/api/v1/items` 替代 |
| `/api/v1/docs?path=` | 保留为语法糖，返回 08 形状 |
| `/api/v1/docs/{slug}` | 移除（slug 不再存在于契约） |
| `/api/v1/person` | 保留不变（永久旁路资源） |
| `/api/v1/timeline` | 保留不变（永久旁路资源） |
| 旧字段 slug/path/status/tags | 从契约移除 |

---

## 6. 非目标（本文）

- 终端 / 纸面如何消费发现层
- 具体 GitHub / VPS 接入字段
- 改写 `content/` 存储格式
- 写 API（C 挂起）

---

## 7. 落地记录

- 2026-08-06：Reassessment 校准富读中枢、挂起 C；Deep Dive 锁定对象模型
- 2026-08-06：Deep Dive（契约形状）6 项决策全部拍板；原地演进为 `/api/v1/items`；`api-read.ts` + 路由层落地
- 2026-08-07：补齐索引 `?kind=` / `?source=` 过滤；校验规则澄清（单独 `source` 视为过滤，单独 `localKey` → 400）
