# 环境变量示例（无密钥）

复制为 `.env.local` 后填写。**不要**把口令、token、session secret、网易 Cookie 提交进 Git。

## 站点主人（ADR 0010）

```
# npm run owner:password 写入哈希（scrypt:N:r:p:salt:key，勿用 $）
ARCHIVE_OWNER_PASSWORD_HASH=
# 随机 32+ 字节；生产必填。local-dev 缺省时用内置开发密钥
ARCHIVE_SESSION_SECRET=
# 可选事故闸：false 时即使 owner 也关终端 edit / Actions
# ARCHIVE_UI_WRITE=false
# 写 API CORS 允许的 Origin（公网建议设为 https://cylf.me）
# ARCHIVE_PUBLIC_ORIGIN=https://cylf.me
```

## Agent 写 API（docs/08 §5.7）

```
# npm run token:generate [--scope thoughts/*]
# JSON：{ "<sha256-hex>": "<scope>" }
ARCHIVE_WRITE_TOKENS=
```

## 网易云 BFF（ADR 0009）

Cookie 文件：仓根 `.netease-cookie`（gitignore）。站点 login ≠ 网易 login。公网 owner 流式需要**部署机**上该文件含 `MUSIC_U`。
