# dependencies
node_modules/

# next.js
.next/
out/

# env files (keep .env.example as the tracked template)
.env*
!.env.example

# NetEase music cookie (ADR 0009; local-dev secret)
.netease-cookie

# Local BGM cache (ADR 0011; sync to deploy, do not commit)
/data/music/
/data/webauthn-credentials.json

# Archive body (ADR 0018) — keep playlist curation + schema tracked
/content/**
!/content/music/
!/content/music/playlists/
!/content/music/playlists/**

# debug
npm-debug.log*

# typescript
*.tsbuildinfo

# local tooling
.learnings/
.thinking-advisor/

# pi runtime state (bg task logs / session sidecars; machine-local)
.pi/

# OS
.DS_Store
Thumbs.db

# code graph
.codegraph/

# smoke-ui artifacts
outputs/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Cursor plan records: not launch content
.cursor/plans/
